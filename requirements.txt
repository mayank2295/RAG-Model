# Web server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Embeddings — fastembed (ONNX) keeps memory low enough for 512MB instances
fastembed==0.5.1

# Vector DB (Pinecone v3+ SDK — Pinecone() constructor + ServerlessSpec)
pinecone==5.4.2

# Scheduling
apscheduler==3.11.0

# HTTP + HTML parsing for fetchers
httpx==0.28.1
beautifulsoup4==4.12.3

# Config
python-dotenv==1.0.1

# OpenRouter (OpenAI-compatible client)
openai==1.59.6
